/**
 * <table>
 * <caption>day20：多线程<caption/>
 * <tr></tr>
 * <tr><th>source code</th>
 *     <th>Description</th></tr>
 *
 * <tr><td><code>SynchronizedTest.java</code></td>
 *     <td>synchronized 锁的基本使用, 列举了 5 种使用方式<td/></tr>
 *
 * <tr><td><code>DeadLock.java</code></td>
 *     <td>“死锁” 的案例<td/></tr>
 *
 * <tr><td><code>ThreadReentry.java</code></td>
 *     <td>线程重入<td/></tr>
 *
 * <tr><td><code>Wait_Notify_Test.java</code></td>
 *     <td>Object成员方法 wait()、notify()、notifyAll()的使用<td/></tr>
 *
 * <tr><td><code>YieldTest.java</code></td>
 *     <td>Thread.yield() 的使用<td/></tr>
 *
 * <tr><td><code>InterruptTest.java</code></td>
 *     <td>线程实例方法 interrupt() 的使用<td/></tr>
 *
 * <tr><td><code>ThreadExit.java</code></td>
 *     <td>线程的退出<td/></tr>
 *
 * <tr><td><code>lockSupportTest.java</code></td>
 *     <td>LockSupport 线程堵塞工具类的使用<td/></tr>
 *
 * <tr><td><code>ReentrantLockTest.java</code></td>
 *     <td>ReentrantLock(可重入锁) 的使用<td/></tr>
 *
 * <tr><td><code>ReadWriteLockTest.java</code></td>
 *     <td>ReentrantReadWriteLock (读写锁) 的使用<td/></tr>
 * <table/>

 */
public class _Main {}
